# Threads Launch Draft

Prompt 모음은 부족했다.

비개발자가 Codex로 agent를 만들려면 필요한 건:

1. 올바른 폴더
2. Codex가 먼저 읽을 `AGENTS.md`
3. synthetic data
4. golden set
5. validation log
6. handoff package

그래서 `Codex Agent Fieldkit`으로 정리했다.

API key 없이:

```bash
make demo
make validate
make validate-report
```

이번 업데이트에는 hero image, Remotion intro video, validation report,
starter-kit template, 보험/서비스 운영 합성 pack, 워크숍 pack까지 넣었다.

Repo:

```text
https://github.com/kimsanguine/codex-agent-fieldkit
```
