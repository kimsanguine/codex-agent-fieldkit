# Validation Log Entry Template

```markdown
| YYYY-MM-DD | `command` | Passed/Failed | What changed and what evidence was observed | Remaining risk or next check |
```

Use one row per meaningful check. Do not replace failed rows with only the final
passing row.
