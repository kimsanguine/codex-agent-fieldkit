repos:
  - repo: local
    hooks:
      - id: codex-agent-fieldkit-validate
        name: codex-agent-fieldkit validate
        entry: make validate
        language: system
        pass_filenames: false
