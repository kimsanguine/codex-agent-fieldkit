# Architecture

```text
synthetic CSV -> simple agent -> demo CLI
                 |
                 v
             golden-set eval
```

The template should stay small enough for a non-engineer to inspect.
